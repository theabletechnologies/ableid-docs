---
id: common
title: Общие методы
---

# Общие методы

Методы, не привязанные к конкретному продукту (AbleID, P2P, Pay). Не требуют сессию и не отправляют вебхуки — ответ приходит синхронно в теле HTTP-ответа.

Авторизация — пара `projectId` + `secret` в теле запроса, как и в остальных методах.

---

## Проверка согласия OneID

**[POST]** `https://{domain}/check/check_oneid` - URL для отправки запроса

Возвращает `true` или `false` — разрешена ли передача персональных данных по указанному ПИНФЛ в системе OneID.

### Тело запроса

```json
{
  "projectId": "yDkeHoHWXVqQ9M_URZUtb",
  "secret": "y1iPwmpVmxOe4RFGvUoVHmPmlQ0nY5",
  "pinfl": "12345678901234",
  "lang": "ru"
}
```

**Описание полей:**

| Поле        | Тип                             | Описание               | Обязательное | Пример                           |
|-------------|---------------------------------|------------------------|--------------|----------------------------------|
| `projectId` | `string`                        | ID проекта AbleID      | Да           | "yDkeHoHWXVqQ9M_URZUtb"          |
| `secret`    | `string`                        | Секретный ключ проекта | Да           | "y1iPwmpVmxOe4RFGvUoVHmPmlQ0nY5" |
| `pinfl`     | `string`                        | ПИНФЛ (ровно 14 цифр)  | Да           | "12345678901234"                 |
| `lang`      | `string` (enum: ru, en, uz, oz) | Язык сообщений ответа  | Нет          | "ru"                             |

### Ответы

#### `Успешный ответ: 201`

```json
{
  "statusCode": 200,
  "type": "SUCCESS",
  "message": "Успешно",
  "data": {
    "allowed": true
  }
}
```

**Описание полей:**

| Поле      | Тип       | Описание                                          | Обязательное | Пример |
|-----------|-----------|---------------------------------------------------|--------------|--------|
| `allowed` | `boolean` | Разрешена ли передача персональных данных в OneID | Да           | true   |

#### `Ошибка: 400`

```json
{
  "statusCode": 10103,
  "type": "FACE_INCORRECT_PINFL",
  "data": null,
  "message": "Некорректный ПИНФЛ"
}
```

#### `Ошибка: 404`

```json
{
  "statusCode": 10202,
  "type": "FACE_PROJECT_NOT_FOUND",
  "data": null,
  "message": "Проект не найден"
}
```

